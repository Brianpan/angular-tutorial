%section{"ng-controller" => "PanelController as panel", "ng-init" => "tab = 1"}
  %ul.nav.nav-pills
    %li{"ng-class" => "{active:panel.isTab(1)}"}
      %a{href: "", "ng-click" => "panel.setTab(1)"}
        List All
    %li{"ng-class" => "{active:panel.isTab(2)}"}
      %a{href: "", "ng-click" => "panel.setTab(2)"}
        New
  .main{"ng-controller" => "MainController as main"}
    .panel{"ng-show" => "panel.isTab(1)"}
      %h3 Projects
      %table.table
        %tr
          %th Name
          %th URL
          %th Power
          %th Edit
          %th Destroy
        %tbody{"ng-repeat" => "project in projects"}
          %tr
            %td {{project.name}}
            %td {{project.url}}
            %td {{project.power}}
            %td 
            %td
    .panel{"ng-show" => "panel.isTab(2)"}
      %h3 New Form
      = render "form_details"
